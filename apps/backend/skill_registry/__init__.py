"""TARS skill registry: a Hermes-aggregated CATALOG of discoverable Claude
Code skills (~90k metadata records, never loaded into a prompt in bulk) and
a small set of INSTALLED skill bundles TARS has actually chosen to use,
stored once in the Obsidian vault. See skill_registry/manager.py for the
single entry point (SkillManager) the rest of the backend should use.
"""
