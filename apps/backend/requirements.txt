# TARS backend — core dependencies (free/local-first required path).
# Optional/paid provider extras are listed in requirements-optional.txt.

fastapi==0.141.1
uvicorn[standard]==0.32.0
pydantic==2.13.4
pydantic-settings==2.6.1
websockets==13.1
aiosqlite==0.20.0
apscheduler==3.10.4
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
httpx==0.27.2
python-multipart==0.0.12
jsonschema==4.23.0

# Windows integration -- required by skills/windows_app.py's `focus`/
# `list_running` actions (win32gui/win32process/win32api window
# enumeration and foreground-window switching). TARS targets Windows only,
# so this is core, not optional.
pywin32==312

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
