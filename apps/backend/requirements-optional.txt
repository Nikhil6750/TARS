# Optional, paid-provider dependencies — never required for the core
# free/local-first TARS runtime (see AGENTS.md § Non-negotiable
# architectural boundaries). Install only if you configure the
# corresponding provider.

# ANTHROPIC_API_KEY + ASSISTANT_PROVIDER=anthropic_api
anthropic==0.69.0

# OTEL_EXPORTER_OTLP_ENDPOINT set (otherwise spans are logged locally via
# app/observability.py's LoggingSpanExporter — no network dependency needed)
opentelemetry-exporter-otlp-proto-http==1.27.0
